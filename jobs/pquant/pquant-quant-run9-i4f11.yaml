apiVersion: batch/v1
kind: Job
metadata:
  name: pquant-quant-run9-i4f11
spec:
  template:
    metadata:
      labels:
        mount-eos: "true"
        mount-cvmfs: "true"
        home: "eos"
        mount-afs: "true"
    spec:
      nodeSelector:
        nvidia.com/gpu.product: NVIDIA-H100-80GB-HBM3

      containers:
      - name: pquant-quant-run9-i4f11
        image: registry.cern.ch/ngt/pytorch:2.3.1
        command:
        - "bash"
        - "-c"
        - |
          source /eos/home-m/majorgen/miniconda3/etc/profile.d/conda.sh &&
          conda activate torch_env &&
          cd /eos/home-m/majorgen/particle_transformer/pquant_experiments &&
          python train_pquant_quant.py \
            2>&1 | tee /eos/home-m/majorgen/particle_transformer/pquant_experiments/results/quant_run9_i4f11/train_$(date +%Y%m%d_%H%M).log
        resources:
          limits:
            nvidia.com/gpu: 1
      restartPolicy: Never
